<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.CharactersFragment">

    <data>

        <variable
            name="viewModel"
            type="com.example.android.marvelicious.ui.CharactersViewModel" />
    </data>


    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ProgressBar
            android:id="@+id/loading_spinner"
            android:layout_width="250dp"
            android:layout_height="250dp"
            android:layout_gravity="center"
            app:isNetworkError="@{safeUnbox(viewModel.eventNetworkError)}"
            app:playlist="@{viewModel.characters}" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/charactersList"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:listitem="@layout/character_item" />
    </FrameLayout>
</layout>